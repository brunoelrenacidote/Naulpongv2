# Host del Worker de Cloudflare (sin esquema, sin barra final).
# En dev local: 127.0.0.1:1999 (lo levanta `wrangler dev` en :1999).
# En producción usá el host que te dio `wrangler deploy`,
# por ejemplo: naulpong.<tu-subdominio>.workers.dev
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
